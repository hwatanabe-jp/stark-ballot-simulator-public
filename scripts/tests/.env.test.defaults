# Test-only fallback values for local/CI harnesses that start Next.js in production mode.
# Existing environment variables and .env.local values take precedence.
SESSION_CAPABILITY_SECRET=local-mock-session-capability-secret-0123456789abcdef
