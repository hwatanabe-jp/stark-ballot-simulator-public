[package]
name = "methods-minimal"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[lib]
doctest = false

[build-dependencies]
risc0-build = { workspace = true }

[package.metadata.risc0]
methods = ["guest-minimal"]
