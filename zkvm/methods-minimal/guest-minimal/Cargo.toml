[package]
name = "guest-minimal"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[[bin]]
name = "guest-minimal"
path = "src/main.rs"

[dependencies]
risc0-zkvm = { version = "3.0.5", default-features = false, features = ["std"] }
serde = { version = "1.0", features = ["derive"] }
